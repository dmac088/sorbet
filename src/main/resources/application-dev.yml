spring:

  mail:
    host: 127.0.0.1
    password: ${SMTPPassword}
    port: 1025
    properties:
        mail:
            smtps:
                auth: true
                starttls:
                    enable: false
                timeout: 80000
            transport:
                protocol: smtps
    protocol: smtps
    username: dmac088@proton.me

  output:
    ansi:
      enabled: ALWAYS

  datasource:
    mochi:
      application: 
        max-lifetime: 45000
        maximum-pool-size: 2
        connection-timeout: 30000
        minimum-idle: 1 
        idleTimeout: 35000
        url: jdbc:postgresql://localhost:5432/mochidb?currentSchema=mochi
        username: mochi_app
        password: password
        driver-class-name: org.postgresql.Driver
      
      owner:
        max-lifetime: 45000
        maximum-pool-size: 2
        connection-timeout: 30000
        minimum-idle: 1
        idleTimeout: 35000
        url: jdbc:postgresql://localhost:5432/mochidb?currentSchema=mochi
        username: mochidb_owner
        password: password
        driver-class-name: org.postgresql.Driver
      
    security: 
      application:
        max-lifetime: 45000
        maximum-pool-size: 2
        connection-timeout: 30000
        idleTimeout: 35000
        minimum-idle: 1
        url: jdbc:postgresql://localhost:5432/mochidb?currentSchema=security
        username: security_app
        password: password
        driver-class-name: org.postgresql.Driver
     
      owner: 
        max-lifetime: 45000
        maximum-pool-size: 2
        connection-timeout: 30000
        idleTimeout: 35000
        minimum-idle: 1
        url: jdbc:postgresql://localhost:5432/mochidb?currentSchema=security
        username: security_owner
        password: password
        driver-class-name: org.postgresql.Driver
    
app:
  globals:
    defaultProductRootCategoryCode: PRM01
    rcURI: /registrationConfirmation
    beURL: https://localhost:8080
    feURL: https://localhost:3000
        
server:
  port: 8080
  ssl:
    key-store-type: PKCS12
    key-store: classpath:mochi.p12
    key-store-password: password
    key-alias: mochi
    
support:
    email: dan@sv2.io

